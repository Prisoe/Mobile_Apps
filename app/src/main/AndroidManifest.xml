<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickBooksApp">

        <!-- HomeActivity as Launcher Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuickBooksApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CreateNoteActivity -->
        <activity
            android:name=".CreateNoteActivity"
            android:exported="true"
            android:label="@string/create_note_title"
            android:theme="@style/Theme.QuickBooksApp" />

        <!-- ViewEditNoteActivity -->
        <activity
            android:name=".ViewEditNoteActivity"
            android:exported="true"
            android:label="@string/view_edit_note_title"
            android:theme="@style/Theme.QuickBooksApp" />

    </application>

</manifest>
